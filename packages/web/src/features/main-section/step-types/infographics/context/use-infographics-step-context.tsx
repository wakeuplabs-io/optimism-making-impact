import { useStepContext } from '@/features/main-section/step-context';

/**
 * Custom hook that provides the current step context.
 */
export function useInfographicsStepContext() {
  return useStepContext();
}
