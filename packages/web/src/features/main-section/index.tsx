import { MainSectionContainer } from '@/features/main-section/container';
import { MainSectionContent } from '@/features/main-section/content';

export function MainSection() {
  return (
    <MainSectionContainer>
      <MainSectionContent />
    </MainSectionContainer>
  );
}
